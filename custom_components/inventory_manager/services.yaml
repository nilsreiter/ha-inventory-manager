consume:
  name: Consume
  target:
    entity:
      domain: number
      supported_features:
        - inventory_manager.InventoryManagerEntityType.SUPPLY

  fields:
    amount:
      name: Amount
      selector:
        number:
    predefined-amount:
      name: Predefined Amount
      selector:
        select:
          translation_key: "time"
          options:
            - "morning"
            - "noon"
            - "evening"
            - "night"